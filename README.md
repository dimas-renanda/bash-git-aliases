# bash-git-aliases ⚙️

Useful git aliases and shell functions. Source in your `.bashrc` or `.zshrc`.

## Install
```bash
echo 'source ~/bash-git-aliases/git_aliases.sh' >> ~/.zshrc
source ~/.zshrc
```
